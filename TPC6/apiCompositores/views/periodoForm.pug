extends layout 

block content 
    .w3-card-4 
        header.w3-container.w3-purple 
            h2 #{title}
        form.w3-container(method="POST")
            fieldset 
                label(for="id") Periodo 
                input.w3-input.w3-round(type="text", name="nome")
            br 
            button.w3-btn.w3-purple.w3-mb-2(type="submit") Add